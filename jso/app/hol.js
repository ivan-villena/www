// WINDOW
'use strict';

// Holon
class _app_hol {

  // operadores
  static val( $tip, $val, $ele ){
    let $_ = "", $={};
    switch( $tip ){      
    }
    return $_;
  }
  // pido Valores
  static val(){
    let $_ = "", $={};
    return $_;
  }
  // pido Ficha
  static fic( $ele, $val, $ope ){
    let $_ = "", $={};
    return $_;
  }
  // pido tablero
  static tab( $ide, $ope={} ){
    let $_ = "", $={};
    return $_;
  }
}